PORT=5000
NODE_ENV=development

# Optional: Google Gemini API key for Library Chatbot & Book Auto-categorization
# If left empty, the application uses its built-in local knowledge engine seamlessly.
GEMINI_API_KEY=
